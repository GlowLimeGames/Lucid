using UnityEngine;
using System.Collections;

public class Progress : MonoBehaviour{

    //LTextGroup texts;
    int ID;

	void Start () 
	{
        ID = 1;
        /*

        LTextGroup textGroup = JsonUtility.FromJson<LTextGroup>(Resources.Load<TextAsset>("Texting/Example").text);

        */
    }



		


	void Update()
	{

	
	}	

    void ChooseA()
    {
        /*
        foreach(LText text in texts)
        {
            if((text.ID).Equals(ID + "") && (text.choice).Equals("A"))
            {
                ProgressResponse pResponse = new ProgressResponse(text);
            }


        }
        */
    }

    void ChooseB()
    {
        /*
        foreach(LText text in texts)
        {
            if((text.ID).Equals(ID + "") && (text.choice).Equals("B"))
            {
                
            }


        }
        */
    }

    void ChooseC()
    {
        /*
       foreach(LText text in texts)
       {
           if((text.ID).Equals(ID + "") && (text.choice).Equals("C"))
           {
                
           }


        }
        */
    }



}
